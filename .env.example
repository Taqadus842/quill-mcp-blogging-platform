# Server
PORT=3000
HOST=127.0.0.1
# Comma-separated key:userId pairs. Example: dev-key:sabeen
MCP_API_KEYS=replace-with-a-long-random-key:sabeen

# Set to http to use the shared Quill API instead of local storage.
BACKEND_MODE=memory
QUILL_API_URL=http://localhost:4000/api
QUILL_API_KEY=
